# Unified Python requirements for native (non-Docker) local development
# Uses exact versions from the service requirements.txt files (Python 3.11 compatible)

# -- Web framework (common to all 3 Python services) --
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
httpx==0.27.0

# -- Doc parser --
pdfplumber==0.11.0
python-multipart==0.0.9
pillow==10.3.0

# -- RAG service --
faiss-cpu==1.8.0
sentence-transformers==3.0.1

# -- Agent orchestrator --
langgraph==0.1.14
langchain==0.2.6
langchain-core==0.2.22

# -- LLM client (shared) --
openai==1.35.3
